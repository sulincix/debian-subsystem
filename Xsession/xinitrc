DESTDIR="$(bash -c 'source /usr/lib/sulin/dsl/variable.sh; echo ${DESTDIR}')"
if [ -z "$XDG_DATA_DIRS" ]; then
    XDG_DATA_DIRS=/usr/local/share/:/usr/share/:$DESTDIR
else
    XDG_DATA_DIRS="$XDG_DATA_DIRS":$DESTDIR
fi
export XDG_DATA_DIRS
#!/bin/bash
if ! ls /tmp/hostctl &>/dev/null ; then
    echo -e "Failed to connect hostctl. Please run \"hostctl-daemon &\" on host"
    exit 1
fi
timeout 3 echo "$*" > /tmp/hostctl || {
    echo -e "Failed to connect hostctl. Please run \"hostctl-daemon &\" on host"
    exit 1

}
